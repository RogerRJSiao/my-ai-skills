# S06 Excel實作 — 逐項評分細則與檢查方法

本文件補充 `assets/S06_Excel實作檔_評閱標準_v0_1.xlsx` 的文字敘述，記錄「怎麼從
.xlsx 檔案裡技術性地驗證每一小項」，以及批改過程中已經遇過的實際案例（正確
做法 / 常見錯誤）。搭配 `scripts/inspect_submission.py` 的輸出一起讀。

18 個小項、大項與滿分（含額外加分）彙總表：

| 小項代號 | 大項 | 基本配分 | 額外加分 |
|---|---|---|---|
| 1.1 | 考題1.薪資 | 5 | - |
| 1.2 | 考題1.薪資 | 10 | - |
| 1.3 | 考題1.薪資 | 10 | - |
| 1.4 | 考題1.薪資 | 5 | - |
| 1.5 | 考題1.薪資 | - | 6 |
| 2.1 | 考題2.成績 | 4 | - |
| 2.2 | 考題2.成績 | 4 | - |
| 2.3 | 考題2.成績 | 9 | - |
| 2.4 | 考題2.成績 | 10（排序5+篩選5） | - |
| 2.5 | 考題2.成績 | 3 | - |
| 2.6 | 考題2.成績 | - | 3 |
| 3.1 | 考題3.車輛銷售 | 5 | - |
| 3.2 | 考題3.車輛銷售 | 15（三等分各5） | - |
| 3.3 | 考題3.車輛銷售 | 5 | - |
| 3.4 | 考題3.車輛銷售 | - | 9（三項各3） |
| 4.1 | 考題4.武將能力 | 5 | - |
| 4.2 | 考題4.武將能力 | 10（拆解5+5） | - |
| 4.3 | 考題4.武將能力 | - | 6（兩項各3） |

基本配分總和 = 100；含額外加分上限 = 124。「分數加總」欄位可以超過100分，
這是正常的（額外加分項的設計）。

---

## 考題1. 薪資（樞紐分析）

**判斷有無樞紐表（1.1 的前提）**：用 `inspect_submission.py` 的
`pivot_薪資` 欄位判斷。它只抓「薪資」工作表 rels 裡真正掛的 pivotTable，
**已經排除【考題】活頁上的範例樞紐表**（因為那個 pivotTable 是掛在
sheet5／考題的 rels 上，不會出現在薪資 sheet 的 rels）。
若 `pivot_薪資` 為 `null`，代表薪資活頁完全沒有樞紐表 → **1.1~1.5 全部 0 分**
（不用再檢查其他小項）。

**1.2 欄列標籤**：看 `col_visible_labels` / `row_visible_labels`。
- 列標籤（row）必須同時有「男」「女」→ 缺一即該半（5分）給0。
- 欄標籤（col）標準答案只顯示「業務」一項；但若同時完整顯示「業務、會計、
  門市」三個部門，屬於【折衷微調】允許的完整呈現方式，一樣給滿分，不算錯誤。
  只要不是「業務」也不是「業務+會計+門市」的其他組合（例如漏列、只列會計），
  才視為錯誤（0或半分視情況判斷，一般沒把握就給0）。

**1.3 彙總方式**：看 `data_fields[0].subtotal`。`"average"` 給滿分；
`"sum"` 給一半；其他給0。

**1.4 貨幣格式**：**不要只看 pivotField 的 numFmtId**——很多檔案
`applyNumberFormats="0"`，代表 pivot 自己的格式設定不會套用，實際顯示格式
要看 pivot 輸出區塊儲存格本身的 `number_format`（`value_number_formats`
欄位）。含 `"$"` 字樣 → 滿分；「通用格式」或純數值（如 `#,##0`）→ 半分；
其他 → 0分。

**1.5 額外加分（來源為表格）**：看 `source_is_named_table`。若
`source_name_or_range` 是一個名稱（如 `"salary"`）而不是像
`"薪資!$A$1:$J$13"` 這種含 `$`/`:` 的儲存格範圍字串，代表樞紐表來源已設為
Excel表格 → 給滿額外加分（6分）。

---

## 考題2. 成績

用 `grades_sheet_info`（腳本 `成績` 欄位）逐項核對：

- **2.1 凍結窗格**（4分）：`freeze_panes == "B3"` → 滿分。如果只固定列
  或只固定欄（例如 `"A3"` 或 `"B1"`），各給一半。
- **2.2 跨欄置中**（4分）：`a1_center_or_merged` 為真（不論是真的合併儲存格
  還是「跨欄置中」對齊樣式，兩種都算數）給一半；`header_text_preserved`
  （A1文字仍是「經濟甲班 期中考成績表」）再給另一半。
- **2.3 公式與範圍**（9分）：`g3_g15_average_formulas_ok`（G3:G15 都用
  AVERAGE）給一半；`row_avg_formulas_ok` 且
  `row_avg_integer_format`（班平均列 B:F 用 AVERAGE 且顯示為整數格式）
  給另一半。**注意**：先用 `avg_row_index` 確認「班平均」是哪一列，不要
  預設一定是第16列。
- **2.4 排序與篩選**（10分＝排序5+篩選5，依【微調】版本）：
  `sorted_descending` → 排序5分；`filter_gt70_applied`（表格的
  customFilter operator=greaterThan, val=70）→ 篩選5分。兩者互相獨立，
  常見錯誤是「只排序、忘記篩選」（`hidden_rows` 應該是 `[11,12,13,14,15]`
  這樣的 5 列被隱藏；如果 `hidden_rows` 是空的，通常代表篩選沒有真的套用）。
- **2.5 框線**（3分）：`borders_complete` 為真才給分，否則0分（依標準沒有
  半分選項，此項是全有全無）。
- **2.6 額外加分（G16無公式）**（3分）：`g_avg_row_cell.is_empty` 為真才
  給分。曾出現的錯誤案例：學生把「個人平均」那欄也往下拉出
  `=AVERAGE(G3:G15)`，這樣 G16 就有值/公式了，不能給這項加分（但不影響
  2.3 的分數，因為 2.3 只看 B:F 欄）。

---

## 考題3. 車輛銷售（動態直條圖）與 考題4. 武將能力（動態雷達圖）

兩題判斷邏輯相同，差別只在圖表類型與座標軸/圖例應顯示的內容。

**第一步：判斷原始資料是否被更動（極重要，會讓整大題歸零）**

用 `--question` 參數提供原始題目檔（`assets/S06_excel_question.xlsx`），
腳本會輸出 `車輛銷售_raw_data_check` / `武將能力_raw_data_check`，比較
A欄類別（廠牌／武將能力屬性）與第2列標頭（月份／武將姓名）是否與原始題目
完全一致。**只要有任何一格不同（`unchanged: false`），該大題（3.1~3.4 或
4.1~4.3）全部給0分**，不用再檢查圖表細節。已知案例：學生把「一月/二月/
三月」改成「十一月/十二月/十三月」，這樣即使圖表技術上仍是動態的，也要
整題归零。

**第二步：確認圖表是動態的**

`is_dynamic` 為真（`c:numRef`/`c:strRef` 存在，代表公式參照儲存格而非
寫死數值）。若 `literal_only` 為真（`numLit`/`strLit`，貼上固定數值），
該大題（3.x 或 4.x）全部0分。

**3.1 / 4.1 作答狀態**（各5分）：資料未被更動 + 圖表為動態 + 圖表類型正確
（`chart_type == "barChart"` 為直條圖／`"radarChart"` 為雷達圖）→ 滿分。

**3.2 座標軸設定**（15分，三等分）：
- 類別（`categories`）需恰好是 `["福特","賓士","裕隆","三菱"]`（順序不拘，
  但四個都要有、不能多也不能少）→ 5分。**常見錯誤**：把類別軸設成月份
  （一月/二月/三月），把廠牌放到圖例——這是把兩個維度對調了，類別軸這5分
  直接是0。
- `y_max == "2000"` 且 `y_major_unit == "500"` 兩者都對才給5分（其中一個
  錯就是0，不是各半）。
- `gridlines_none` 為真 → 5分。

**3.3 圖例設定**（5分）：`series_names` 需恰好是
`["一月","二月","三月"]`（順序不拘），少一個或多一個都是0分（此項沒有
半分）。

**3.4 額外加分**（9分，三項各3）：
- `has_data_labels` → +3（注意題目特別註明：如果學生用的是「資料表
  (Data Table)」而不是逐點資料標籤，兩者要區分清楚，`dLbls` 的
  `showVal` 才算一般資料標籤；資料表功能是另一個 XML 節點
  `plotArea/dTable`，目前腳本沒有偵測，遇到時人工確認）。
- `has_chart_title` → +3。
- `has_x_axis_title` 且 `has_y_axis_title`（兩者都要有才算，因為原題目
  要求「縱軸的數量」和「橫軸的廠牌」都要標）→ +3。

**4.2 座標設定**（10分，拆解5+5）：
- 類別軸（`categories`）需恰好是
  `["統帥","武力","智力","政治","魅力"]` → 5分（全對才給，任一缺漏0分）。
- 圖例（`series_names`）需恰好是 `["趙雲","周瑜","程遠志","劉禪"]` → 5分
  （原始資料裡「程遠志」字串可能帶有一個 tab 字元 `\t`，比對時建議先
  `.strip()` 再比較，避免因為看不見的空白字元誤判）。

**4.3 額外加分**（6分，兩項各3）：
- `has_chart_title` → +3。
- 使用含資料點標記的雷達圖：檢查 `marker_symbols`，如果全部是
  `"none"` 代表雖然 `radarStyle` 可能設為 `"marker"`，但每個資料點的
  `c:symbol` 都是 none，等於沒有真的顯示標記 → 不給分。要看到
  `marker_symbols` 出現 `"circle"`／`"diamond"` 等非 none 的值才給分。

---

## 已知案例對照（供 sanity check）

| 檔案 | 薪資 | 成績 | 車輛銷售 | 武將能力 | 總分 |
|---|---|---|---|---|---|
| M2120-242958-蕭瑞展 | 36/36 | 33/33 | 25/25 | 15/15 | 109 |
| M2120-242000-建大員林 | 36/36 | 28/33（篩選未做） | 15/25（座標軸/圖例對調） | 15/15 | 94 |
| M2120-242000-林建大 | 0/36（無樞紐表） | 22/33（無框線、篩選未做、G16有公式） | 0/25（原始資料被改） | 15/15 | 37（不合格）|

這三個案例可以當作迴歸測試：改動 `inspect_submission.py` 後，重新對這三個
檔案跑一次，結果應該與上表一致。
