---
name: s06-excel-grading
description: Standard operating procedure for grading "S06" Excel實作 (pivot table + charts) student submissions against the bundled 評閱標準/答案/原始題目 files, and writing results into the 評分結果 workbook (活頁1 明細 + 活頁2 結果表). Use this whenever the user uploads one or more student .xlsx submissions named 「部門代號-工號-姓名.xlsx」 and asks to grade/評分/批改 an "S06" or "Excel實作" assignment, wants scores added to an existing 評分結果 file, or references 評閱標準.xlsx / S06_excel_answer.xlsx / S06_excel_question.xlsx. Trigger this even if the user just says "幫我評分這個檔案" in a conversation that has already established the S06 Excel grading context.
---

# S06 Excel實作 評分 SOP

批改「薪資樞紐分析 / 成績表 / 車輛銷售直條圖 / 武將能力雷達圖」四大題、共18個
小項的 Excel 實作考題，並把結果寫進標準格式的『評分結果』活頁簿。

## 這個 skill 涵蓋的固定資產（assets/）

- `S06_Excel實作檔_評閱標準_v0_1.xlsx` — 評分標準表（大項/小項/評分標準/配分/
  額外加分配分）。**這是評分的唯一依據**，如果使用者這次提供了不同版本的評閱
  標準檔案，以使用者提供的為準，並比對是否有配分變動。
- `S06_excel_question.xlsx` — 原始題目檔（未作答），用來比對「原始資料有沒有
  被更動」。
- `S06_excel_answer.xlsx` — 標準解答檔，可作為「正確答案長什麼樣子」的參考，
  但**評分永遠以評閱標準表的文字敘述為準**，不是拿學生檔案跟答案檔案做逐格
  diff（答案檔案本身的呈現方式不一定是唯一正確解，例如 1.2 欄標籤兩種呈現
  方式都合格）。
- `S06_Excel實作檔_評分結果_v0_1.xlsx` — 空白輸出模板，兩個活頁的表頭固定
  格式如下：
  - 活頁1「評分結果明細」：截止日/課程代碼/實作項目/部門代號/工號/姓名/
    大項/小項代號/得分/完成比例/執行者/評閱結果說明
  - 活頁2「評分結果表」：截止日/課程代碼/實作項目/部門代號/工號/姓名/檔案數/
    分數加總/是否合格/執行者/評閱結果簡述(150字以內)

詳細的逐小項技術檢查方法、常見錯誤案例、已知檔案的評分結果對照表，都在
`references/scoring_checks.md`，開始評分前先讀過一遍。

## 流程

### 第0步：確認缺漏的「無法從檔名取得」的資訊

檔名格式「部門代號-工號-姓名.xlsx」只能解析出部門代號/工號/姓名三個欄位。
以下欄位**無法從任何檔案自動取得**，必須向使用者確認（如果對話中已經提供過，
直接沿用，不用重複問）：

- 截止日
- 課程代碼／課程名稱（例如 "S06"）
- 實作項目（例如 "Excel實作"）
- 執行者（評閱者姓名或代號）

如果這是一次全新的評分任務且以上任一項缺漏，先用一次提問把它們問齊，再開始
批改。如果是「幫既有結果表追加學生」的情境（活頁簿已存在），直接讀取既有
活頁2任一列的這四個欄位沿用即可，不用重問。

同時檢查每個上傳檔案的檔名是否符合「部門代號-工號-姓名.xlsx」格式：
- 檔名用 `-` 切開應該剛好是三段（副檔名前）。
- 如果檔名不符合格式（段數不對、看起來像是打錯字），停下來跟使用者確認
  正確的部門代號/工號/姓名，不要自己猜測或用檔名硬湊。

### 第1步：讀取評閱標準

用 `openpyxl`/`markitdown` 讀 `assets/S06_Excel實作檔_評閱標準_v0_1.xlsx`
（或使用者本次提供的版本），取出大項、小項代號（如 "1.1"）、配分、額外加分
配分。18 個小項與滿分彙總已經整理在 `references/scoring_checks.md` 最上方
的表格，可以直接核對、不用每次重新解析。

### 第2步：逐一批改每個學生檔案

對每個學生 .xlsx：

1. 用 `scripts/inspect_submission.py <學生檔案> --question assets/S06_excel_question.xlsx`
   跑一次，拿到結構化 JSON（pivot table 設定、圖表動態性與座標軸/圖例內容、
   凍結窗格、合併/跨欄置中、框線、排序/篩選、公式）。
2. 對照 `references/scoring_checks.md` 的規則，逐一判斷 18 個小項的得分，
   並用一兩句話寫出「評閱結果說明」（未完成的欠項，或已完成的加分項——
   對照原本的評分標準文字，講清楚具體原因，不要只寫「正確」兩個字）。
3. 特別注意兩個「連坐」規則，先檢查、可以省掉後面逐項檢查的力氣：
   - 薪資活頁完全沒有樞紐表（且要排除【考題】活頁本身的範例）→ 1.1~1.5
     全部0分。
   - 車輛銷售／武將能力的 A欄或第2列原始資料被更動 → 對應大題(3.x 或 4.x)
     全部0分，不論圖表其他設定是否正確。
4. 完成比例 = 得分 / 該小項配分（含額外加分小項則除以額外加分上限），存成
   0%~100% 的百分比格式。

### 第3步：彙整活頁2並產出檔案

- 「分數加總」用公式（例如
  `=SUMIF(評分結果明細!$F$2:$F$<末列>,F<列>,評分結果明細!$I$2:$I$<末列>)`）
  加總該姓名在活頁1所有列的得分，不要手動加完寫死數字——這樣之後如果活頁1
  有調整還能自動重算。
- 「是否合格」用公式判斷 `>=60` 顯示「合格」否則「不合格」。
- 「評閱結果簡述」150字以內：約100字寫優點、約50字寫待改善（如果沒有待改善
  項目可以省略後半段）。內容要具體對應到该學生實際的得分分布，不要寫套話。
- 輸出檔名慣例：`<課程代碼>_<實作項目>_評分結果_<YYMMDD>.xlsx`（YYMMDD 用
  今天日期，或使用者指定的檔名）。

### 第4步：如果是「追加學生」到既有結果檔

如果使用者提供的是已經有資料的評分結果檔（而不是空白模板），用 openpyxl
讀取兩個活頁目前的 `max_row`，從下一列開始 append，**不要**覆蓋既有列。
append 完後，記得把活頁2所有列（含原本就有的）的 SUMIF 公式範圍
（`$F$2:$F$<末列>` 那段）更新成涵蓋新增後的活頁1末列，否則舊學生的加總會
漏掉新資料（活頁1本來就沒新增列的話則不受影響，但統一重寫一次比較保險）。

### 第5步：驗證與交付

- 用 xlsx skill 的 `recalc.py` 重新計算公式，確認 `status: success` 且
  `total_errors: 0`。
- 用 openpyxl 以 `data_only=True` 讀回結果，跟自己算的分數/合格判定核對一次
  再交付，避免公式範圍寫錯導致加總對不上。
- 存到 `/mnt/user-data/outputs/`，用 `present_files` 交付，並在對話中用
  條列式簡短說明每位學生的總分、合格與否、以及扣分主因（不用整份 12欄 x
  18列的明細貼在對話裡，那些都在檔案裡了）。

## 何時要人工介入、不要自己硬判

- 3.4「運算列表資料標籤」是否為圖表下方的「資料表(Data Table)」功能，
  `inspect_submission.py` 目前偵測不到 `plotArea/dTable` 這個節點，遇到疑似
  資料標籤但腳本回報 `has_data_labels: false` 時，打開 chart XML
  （`xl/charts/chartN.xml`）人工確認一次。
- 如果評閱標準表本身跟這次使用者提供的版本配分不同（例如日後調整），以
  使用者這次提供的檔案為準，並在回覆中提醒使用者配分已更新。
- 檔名解析不出部門代號/工號/姓名，或一個檔案裡找不到預期的四個工作表
  （薪資/成績/車輛銷售/武將能力），先跟使用者確認檔案是否正確，不要憑空
  帶入預設值。
